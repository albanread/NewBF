//! `newbf-test-matrix` — test orchestration.
//!
//! Drives the curated Beef sample corpus (`E:\NewBF\beef-tests\`) through
//! the full pipeline and captures the per-phase reports, so a sprint can
//! declare exactly which samples and which reports it makes pass. Shared
//! shape with NewM2's `newm2-test-matrix`.
