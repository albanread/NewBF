[package]
name = "newbf-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "NewBF: parser — tokens into a parse tree, reduced to an AST."

[dependencies]
newbf-lexer.workspace = true

[lints]
workspace = true
