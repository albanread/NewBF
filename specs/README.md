# specs/

Per-sprint specifications, one file per sprint (`NN-name.md`), expanding
the sprint stubs in [`../SPRINTS.md`](../SPRINTS.md) into concrete
deliverables, acceptance criteria, and report schemas before the sprint
starts. Mirrors the `specs/` convention in NewOpenDylan.

(Empty until Sprint 02 is specced.)
